# iops = int(
    #     "".join(i for i in x if i.isdigit())
    # )  # sometime the value is in 2 digit and in some rare case its in 1 digit so, or in some case it will combination of alpha,some special charter with number so we will only pick digit out of it.
    # return digi